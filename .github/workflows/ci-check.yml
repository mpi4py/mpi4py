name: ci-check

on:  # yamllint disable-line rule:truthy
  schedule:
    - cron: '0 3 * * 0'
  workflow_call:
    inputs:
      py:
        description: 'Python version'
        required: false
        default: 3
        type: string
  workflow_dispatch:
    inputs:
      py:
        description: 'Python version'
        required: true
        default: 3
        type: string

permissions:
  contents: read

jobs:

  conf:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest

    steps:

    - name: Checkout
      uses: actions/checkout@v7
      with:
        persist-credentials: false

    - name: Setup Python (${{ github.event.inputs.py || 3 }})
      uses: actions/setup-python@v7
      with:
        python-version: ${{ github.event.inputs.py || 3 }}
        check-latest: true
        cache: pip
    - run: python -m pip install --group build

    - name: Build package
      run:  python setup.py build
      env:
        MPICFG: nompi
        CFLAGS: -O0

    - name: Check build configuration
      run:  |
        # check nompi pympiconf.h
        cp "src/lib-mpi/pympiconf.h" "conf/nompi/pympiconf.h"
        git diff --exit-code

    - name: Check code generation
      run:  |
        python conf/mpiapigen.py
        git diff --exit-code

  docs:
    runs-on: ubuntu-latest

    steps:

    - name: Checkout
      uses: actions/checkout@v7
      with:
        persist-credentials: false

    - name: Setup Python (${{ github.event.inputs.py || 3 }})
      uses: actions/setup-python@v7
      with:
        python-version: ${{ github.event.inputs.py || 3 }}
        cache: pip
    - run: python -m pip install --group docs

    - name: Install package for documenting
      run:  python -m pip install .
      env:
        MPICFG: nompi-fast
        CFLAGS: -O0

    - name: sphinx-build coverage
      run:  sphinx-build -M coverage docs/source build -q -E -W -jauto

    - name: check coverage
      run:  |
        coverage=build/coverage/python.txt
        pattern='(classes|functions|methods):'
        if grep -qE $pattern $coverage; then
            cat $coverage; exit 1;
        fi

  lint:
    runs-on: ubuntu-latest

    steps:

    - name: Checkout
      uses: actions/checkout@v7
      with:
        persist-credentials: false

    - name: Setup Python (${{ github.event.inputs.py || 3 }})
      uses: actions/setup-python@v7
      with:
        python-version: ${{ github.event.inputs.py || 3 }}
        cache: pip
    - run: python -m pip install --group lint

    - name: Install package for linting
      run:  python -m pip install .
      env:
        MPICFG: nompi-fast
        CFLAGS: -O0

    - name: ruff check
      run:  ruff check

    - name: ruff format
      run:  ruff format --diff

    - name: pylint
      run:  pylint mpi4py

    - name: codespell
      run:  codespell

    - name: cython
      run:  conf/cythonize.sh -Wextra -Werror

    - name: cython-lint
      run:  cython-lint .

    - name: vermin
      run:  vermin .

    - name: yamllint
      run:  yamllint .

    - name: shellcheck
      run:  sh .ci/shellcheck.sh

  type:
    runs-on: ubuntu-latest

    steps:

    - name: Checkout
      uses: actions/checkout@v7
      with:
        persist-credentials: false

    - name: Setup Python (${{ github.event.inputs.py || 3 }})
      uses: actions/setup-python@v7
      with:
        python-version: ${{ github.event.inputs.py || 3 }}
        cache: pip
    - run: python -m pip install --group type

    - name: Install package for typing
      run:  python -m pip install .
      env:
        MPICFG: nompi-fast
        CFLAGS: -O0

    - name: Check stub generation
      run:  |
        python conf/mpistubgen.py
        git diff --exit-code

    - name: mypy (typecheck)
      run:  |
        mypy --python-version 3.15 -p mpi4py
        mypy --python-version 3.14 -p mpi4py
        mypy --python-version 3.13 -p mpi4py
        mypy --python-version 3.12 -p mpi4py
        python -m pip install -qU --group type 'numpy<2.5'
        mypy --python-version 3.11 -p mpi4py
        mypy --python-version 3.10 -p mpi4py
        python -m pip install -qU --group type

    - name: mypy (stubtest)
      run:  stubtest mpi4py
