.PHONY: default

default:
#	${MAKE} -C compute-pi
	${MAKE} -C cython
	${MAKE} -C embedding
	${MAKE} -C f2py
	${MAKE} -C init-fini
	${MAKE} -C mandelbrot
	${MAKE} -C mpi-ref-v1
	${MAKE} -C spawning
	${MAKE} -C swig
	${MAKE} -C threads
