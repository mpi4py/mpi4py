{% set build = 0 %}
{% set mpi = environ['MPI'] or 'mpich' %}
{% set req = {'mpich':'>=3.1', 'openmpi':'>=1.8'} %}

package:
  name: mpi4py
  version: 2.0.0.dev

about:
  home: https://bitbucket.org/mpi4py/
  summary: 'Python bindings for MPI'
  license: 'BSD (https://bitbucket.org/mpi4py/mpi4py/src/master/LICENSE.rst)'

source:
# git_url: https://bitbucket.org/mpi4py/mpi4py
# git_rev: master
  path: ../../..

requirements:
  build:
    - python
    - cython >=0.22
    - {{mpi}} {{req[mpi]}} # [not win]
  run:
    - python
    - {{mpi}} {{req[mpi]}} # [not win]

build:
  number: {{build}}
  string: py{{py}}_{{mpi}}_{{build}}
  script: python setupegg.py build_exe install -O0

test:
  imports:
    - mpi4py
    - mpi4py.rc
    - mpi4py.dl # [not win]
    - mpi4py.MPI
